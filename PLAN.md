# PLAN.md

The original "path to 100% ready" plan (R1–T7) is complete and archived in
`docs/archive/AUDIT_TRIAGE.md` context. Current workstream:

1. [x] Import-graph audit of pages, APIs, components, libs, dependencies
2. [x] Remove provably-dead code (components / libs / API routes / deps)
3. [x] Consolidate duplicate routes and surfaces
4. [x] Make `/flow` a working social feed
5. [x] Wire `/workflows` to server persistence
6. [x] Fix broken user flows (workspace/create, /nav links, seed exposure)
7. [x] Documentation truth-pass (README, .env.example, PROJECT_MAP, docs/archive)
8. [ ] Optional next: e2e coverage for flow social actions; CI workflow re-enable
