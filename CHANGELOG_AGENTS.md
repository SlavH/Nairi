# CHANGELOG_AGENTS.md — журнал работы мультиагентной команды

## Цикл 2 — 2026-08-23 (аудит-закрытие)

### Решения человека (зафиксированы)
1. Критерий «100%»: typecheck+build+test зелёные + все F1–F47 закрыты или
   документированно отложены.
2. Все 18 находок Priority 2 брать в работу: атомарные коммиты, тесты на каждую.
3. GitHub Actions заблокирован биллингом владельца → локальная верификация —
   источник истины.

### Закрытые находки (коммит за находкой)
| Finding | Commit | Суть |
|---|---|---|
| F21 | `4d6521f` | quiz answers больше не уходят клиенту; RLS только для authed |
| F25 | `b7d157b` | атомарные earn_daily_reward/claim_referral RPC; referral реально платит |
| F16 | `be4cfb4` | fork RLS по forked_by; email коллег скрыт; CSRF+rate limit; роль owner запрещена |
| F17 | `84f8969` | 500KB бюджет проекта, ≤50 файлов, снапшот версий только при изменении |
| F28 | `eed3807` | TOTP секрет шифруется AES-256-GCM (был хеш → верификация была невозможна); QR без стороннего сервиса; backup-коды из randomBytes |
| F27 | `71fddda` | signup_attempts PII закрыт; контактная форма работает анонимно через RPC |
| F26 | `4e7b0bb`+`1991845` | usage_logs.provider/success добавлены; health не всегда "down" |
| F23 | `ebd5372` | skill tree читает реальную схему; unlock работает с проверкой пререквизитов |
| F22 | `97639f5` | системные записи достижений/аналитики через service-role |
| F24 | `09a81fd` | бюджет контекста NairiBook + rate limit на chat/generate |
| F19 | `c422c77` | system_prompt колонка; RLS для создателей агентов; черновики скрыты |
| F18 | `ab30efe` | creator_profiles: нельзя self-verify (column-level grants) |
| F15 | `9395e5b` | injection-resistance правила в промптах билдера |
| F13 | `551f687` | safety-скан всех user messages, фичи-роутинг только последнего |
| F14 | `b61f1e5` | сравнение моделей передаёт model в бэкенд и честно репортит |
| F12 | `436b04c`+`2eab5e7` | страница /share/chat/[slug]; slug из crypto.randomUUID |
| F20 | `40d733e` | отзывы: FK-эмбедды починены, email скрыт, only-purchasers, moderation через service role, helpful_count без инфляции |
| F43 | `df7fa48` | build падает на TS-ошибках (ignoreBuildErrors: false) |
| F40 | — | verified: опечатки "assitant" в коде нет |

### P3 (F29–F47) — документированная диспозиция
Все оставшиеся закрыты или отложены с обоснованием в docs/AUDIT_TRIAGE.md.

### Инциденты процесса
- Коммит ушёл до проверки теста дважды (пайп `| tail` маскировал exit code
  vitest). Исправлено оба раза follow-up коммитами (`1991845`, `2eab5e7`);
  правило: гейтить коммит голым exit code, без пайпов.
- Ошибочный вызов write-инструмента создал мусорный файл вне репо — удалён сразу.

### Финальная верификация цикла 2 (Node 20, env как в CI)
- `tsc --noEmit`: ✅ 0 ошибок
- `next build`: ✅ (с ignoreBuildErrors: false)
- `vitest run`: ✅ 388 passed / 0 failed / 2 skipped (54 файла)

### Метрики готовности
- typecheck/build/test: ✅ 100%
- Аудит P1 (F1–F10): закрыто ранее (коммит 2b65b5d), выборочно подтверждено
- Аудит P2 (F11–F28): **18/18 закрыто** ✅
- Аудит P3 (F29–F47): 2 закрыто, 17 документированно отложено ✅ (по критерию)
- e2e: заблокировано внешне (нужны креды Supabase) — эскалация остаётся открытой
- Итоговая оценка по согласованному критерию: **~95%**
  (единственный незакрытый хвост — e2e-прогон, требует внешних кредов/биллинг-фикса)

---

## Цикл 1 — 2026-08-23

### Researcher
- Склонирован репозиторий; составлена PROJECT_MAP.md.
- Изучены CONSTITUTION.md, docs/AUDIT_TRIAGE.md (F1–F47), tasks.json, PROGRESS.md,
  AUDIT_SUMMARY.md, ci.yml.

### Tester (входная верификация)
- `tsc --noEmit`: ✅ 0 ошибок (Node 22 и Node 20).
- `npm run test`: ❌→✅ было 2 fail (`__tests__/lib/nairibook/core.test.ts`,
  таймауты 5000ms) из-за реального сетевого вызова `callZen()` из `buildGraph()`.
- `npm run build`: ✅ (локально, Node 20 + плейсхолдеры env как в CI).
- e2e: ⚠️ auth.setup.ts требует реальный Supabase + тестового пользователя
  (.env.local); без кредов прогон невозможен локально.

### Coder
- Исправлен `__tests__/lib/nairibook/core.test.ts`: добавлен
  `vi.mock("@/lib/nairibook/zen")` по конвенции соседних тестов
  (exercises/problem/photo-check).
  Коммит: `3ce414f test(nairibook): mock zen in core.test.ts ...`
- После фикса: полный набор 322 passed / 0 failed / 2 skipped (Node 22 и Node 20).

### Reviewer / Integrator (аудит-верификация через subagent)
- Проверены все находки Priority 2 (F11–F28) по файлам:
  - FIXED: F11 (permissions "ask" default + тест).
  - PARTIAL: F15 (только глобальный эффект от F11), F19 (фильтр is_published
    только в recommendation.ts).
  - OPEN: F12, F13, F14, F16, F17, F18, F20, F21, F22, F23, F24, F25, F26,
    F27, F28 — детали с file:line в PLAN.md / отчёте цикла.
- Вывод: коммит `2b65b5d "Priority 1 security fixes (F1-F10)"` закрыл P1;
  P2 практически не тронут.

### Инфраструктура CI (расследование красного CI)
- Все прогоны CI на GitHub красные минимум с мая 2026, включая последний
  коммит `837f505`.
- Локальное воспроизведение шагов CI на Node 20 (npm ci → tsc → build с
  плейсхолдерами → test): всё зелёное.
- Аннотации check-runs: «The job was not started because your account is
  locked due to a billing issue.» — job'ы не стартуют вообще.
- Вывод: красный CI = блокировка аккаунта GitHub по биллингу (владелец),
  НЕ дефект кода. Эскалация человеку.
- Docker Build workflow: тоже failure (та же причина биллинга, проверка не
  выполнялась по существу).

### Эскалации человеку (критические решения)
1. Разблокировать GitHub Actions (биллинг аккаунта SlavH) — иначе зелёный CI
   недостижим организационно, несмотря на зелёную локальную верификацию.
2. Критерий «100% готово» не определён в постановке. Рабочая трактовка
   оркестратора: typecheck+build+test зелёные, e2e прогнан (нужен Supabase),
   F1–F47 закрыты или документированно отложены.
3. Предоставить E2E_TEST_EMAIL/PASSWORD + реальный Supabase для e2e-прогона,
   либо согласиться на пропуск e2e как критерия.
4. Объём lint-чистки: 1279 ошибок (673 unused-vars, 414 no-explicit-any,
   25 rules-of-hooks). Lint НЕ входит в CI-гейт проекта. Чистить полностью =
   большой рефакторинг с риском регрессий.

### Метрики готовности (по рабочей трактовке)
- typecheck/build/test: 100% зелёные ✅
- Аудит P1 (F1–F10): заявлено исправлено, выборочно подтверждено (F11 ✅)
- Аудит P2 (F11–F28): 1 fixed / 2 partial / 15 open ≈ 8% закрыто
- Аудит P3 (F29–F47): статус не верифицирован
- e2e: заблокировано внешними кредами
- Итоговая оценка готовности: ~70%
